/** @format */

export const selectMakes = state => state.params.makes;

export const selectMin = state => state.params.min;

export const selectMax = state => state.params.max;

export const selectIsLoading = state => state.params.isLoading;
